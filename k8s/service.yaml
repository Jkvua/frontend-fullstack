apiVersion: v1
kind: Service
metadata:
  name: provafinalfront-devops
spec:
  selector:
    app: provafinalfront-devops
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30444
